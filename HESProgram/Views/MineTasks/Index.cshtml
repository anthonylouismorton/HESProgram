@model IEnumerable<HESProgram.Models.MineTask>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.taskDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Respirator)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MedicalMonitoring)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.taskDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Respirator)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MedicalMonitoring)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.taskId }) |
            @Html.ActionLink("Details", "Details", new { id=item.taskId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.taskId })
        </td>
    </tr>
}

</table>
